# Behavior

Status: Implemented

The Behavior organ compiles dynamic directives into system prompt injections. It is decoupled from the hardcoded `ME_PROFILE` of Siduri, reading directives from the companion's config and memory.
