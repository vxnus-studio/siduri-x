{
  "name": "siduri-x",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 25",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "release:check": "node cli/dist/release-check.js",
    "sync:versions": "node scripts/sync-versions.mjs",
    "sync:check": "node scripts/sync-versions.mjs --check"
  },
  "devDependencies": {
    "turbo": "^1.11.2",
    "typescript": "^5.3.3"
  }
}
